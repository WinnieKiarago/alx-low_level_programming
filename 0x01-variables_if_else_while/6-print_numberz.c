#include <stdio.h>
/**
 * main - program that prints single numbers of base 10 starting from 0
 *
 * Return: Always return 0 (Success)
 */
int main(void)
{
	int a;

        for (a = 0; a < 10; a++)
		putchar (a);
	putchar('\n');
        return (0);
}
